# examples/

Phase 0 暂无 runnable 样例。后续阶段将放：

- `excel-import/legacy.xlsx` + 期望输出 — Excel 导入的 golden sample
- `tiny-soc/` — 完整的最小 demo 项目（config.yaml + tables/ + schemas/）

如需立刻试用 Excel 导入，参见 `../README.md` 的 Quickstart。
