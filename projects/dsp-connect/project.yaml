name: dsp-connect
type: demo
description: Software debugging framework — DWARF symbol-level memory viewer for embedded/DSP targets
language: c
tags: [dwarf, debugger, embedded, dsp, elf, transport, telnet, softprobe]
status: active
difficulty: advanced
created: 2026-04-10
run: cmake -B build && cmake --build build && ./build/dsc_demo
test: cmake -B build && cmake --build build && ctest --test-dir build --output-on-failure
