{
  "description": "FPGA 冒烟流水线：拉合并产物 → bundle 脚本（地址转换、签名）→ 跑冒烟入口",
  "variables": {
    "pkg_dir":     "${workdir_base}/${version}-${commit_short}",
    "report_path": "${workdir_base}/${version}-${commit_short}/report.json"
  },
  "tasks": [
    {
      "name": "pull",
      "order": 1,
      "usage": "artifact-cli pull hw-${platform}-${version}-${commit_short}.tar.gz -o ${pkg_dir}",
      "keyword": [
        { "type": "success", "word": "downloaded" },
        { "type": "error",   "word": "404|ERROR" }
      ]
    },
    {
      "name": "extract",
      "order": 2,
      "usage": "mkdir -p ${pkg_dir}/work && tar xzf ${pkg_dir}/hw-*.tar.gz -C ${pkg_dir}/work"
    },
    {
      "name": "address_remap",
      "order": 3,
      "usage": "platforms/fpga/bundle/remap.sh ${pkg_dir}/work --mode smoke",
      "keyword": [
        { "type": "success", "word": "Remap done" },
        { "type": "error",   "word": "ERROR" }
      ]
    },
    {
      "name": "sign",
      "order": 4,
      "usage": "python platforms/fpga/bundle/sign.py ${pkg_dir}/work",
      "keyword": [
        { "type": "error", "word": "ERROR|Traceback" }
      ]
    },
    {
      "name": "smoke-run",
      "order": 5,
      "usage": "platforms/fpga/smoke/run.sh ${pkg_dir}/work --report ${report_path}",
      "keyword": [
        { "type": "error",   "word": "FATAL" },
        { "type": "success", "word": "SMOKE COMPLETE" }
      ]
    }
  ]
}
